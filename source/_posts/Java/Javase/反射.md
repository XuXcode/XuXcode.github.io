---
title: 反射
date: 2025-12-19
description: 本文深入讲解 Java 反射机制，包括 Class 类、获取成员变量、方法调用等核心操作。
categories:
  - Java
  - Javase
tags:
  - java/advanced
  - 反射
  - Class
---
# 反射

## 认识反射

+ 反射允许对封装类的字段(成员变量)，方法(成员方法)和构造函数(构造方法)的信息进行编程访问。

## 获取class对象的三种方式

+ ```java
  Class.forName("全类名");
  ```

+ ```java
  类名.class

+ ```java
  对象.getClass();
  ```

```Java
//1.Class这个类里面的静态方法forName
//最为常用的 
//Class.forName("类的全类名")： 全类名 = 包名 + 类名
Class clazz1 = Class.forName("com.itheima.reflectdemo.Student");
//源代码阶段获取 --- 先把Student加载到内存中，再获取字节码文件的对象
//clazz 就表示Student这个类的字节码文件对象。
//就是当Student.class这个文件加载到内存之后，产生的字节码文件对象


//2.通过class属性获取
//类名.class
//一般更多的是当成参数传递
Class clazz2 = Student.class;

//因为class文件在硬盘中是唯一的，所以，当这个文件加载到内存之后产生的对象也是唯一的
System.out.println(clazz1 == clazz2);//true


//3.通过Student对象获取字节码文件对象
//当我们已经有了这个类的对象时，才可以使用
Student s = new Student();
Class clazz3 = s.getClass();
System.out.println(clazz1 == clazz2);//true
System.out.println(clazz2 == clazz3);//true
```

## 反射获取构造方法

### Class 类中用于获取构造方法的方法

| 方法名                                                       | 说明                                                         |
| ------------------------------------------------------------ | ------------------------------------------------------------ |
| `Constructor<?>[] getConstructors()`                         | 返回所有公共构造方法对象的数组（仅包含 public 构造函数）     |
| `Constructor<?>[] getDeclaredConstructors()`                 | 返回类中所有构造方法对象的数组（包括 private、protected、default 和 public） |
| `Constructor<T> getConstructor(Class<?>... parameterTypes)`  | 根据指定的参数类型，返回一个公共构造方法对象                 |
| `Constructor<T> getDeclaredConstructor(Class<?>... parameterTypes)` | 根据指定的参数类型，返回一个任意访问权限的构造方法对象（可访问私有构造） |

### Constructor 类中用于创建对象的方法

| 方法名                              | 说明                                                 |
| ----------------------------------- | ---------------------------------------------------- |
| `T newInstance(Object... initargs)` | 根据该构造方法创建对象实例，传入对应参数             |
| `void setAccessible(boolean flag)`  | 设置是否跳过访问检查；设为 `true` 可访问私有构造方法 |

## 反射获取成员变量

### Class类中用于获取成员变量的方法

| 方法名                                | 说明                                                         |
| ------------------------------------- | ------------------------------------------------------------ |
| `Field[] getFields()`                 | 返回公共成员变量对象的数组（只能获取 `public` 的字段）       |
| `Field[] getDeclaredFields()`         | 返回所有成员变量对象的数组（包括 `private`、`protected`、`default` 和 `public`，只要在当前类中定义） |
| `Field getField(String name)`         | 返回指定名称的单个公共 成员变量对象（只能获取 `public` 的字段） |
| `Field getDeclaredField(String name)` | 返回指定名称的单个成员变量对象（可获取任意访问权限的字段，只要存在） |

### Field类中用于创建对象的方法

| 方法名                            | 说明   |
| --------------------------------- | ------ |
| void set(Object obj,Object value) | 赋值   |
| Object get(Object obj)            | 获取值 |

## 反射获取成员方法

### Class 类中用于获取成员方法的方法

| 方法名                                                       | 说明                                                         |
| ------------------------------------------------------------ | ------------------------------------------------------------ |
| `Method[] getMethods()`                                      | 返回所有公共成员方法对象的数组，**包括从父类继承的公共方法** |
| `Method[] getDeclaredMethods()`                              | 返回当前类中所有成员方法对象的数组**（不包括继承的方法）**，包含私有、保护、默认和公共方法 |
| `Method getMethod(String name, Class<?>... parameterTypes)`  | 根据方法名和参数类型，返回一个公共成员方法对象（仅限 public 方法） |
| `Method getDeclaredMethod(String name, Class<?>... parameterTypes)` | 根据方法名和参数类型，返回一个任意访问权限的成员方法对象（可访问 private 方法） |

### Method类中用于调用方法的方法

| 方法名                                      | 说明                                            |
| ------------------------------------------- | ----------------------------------------------- |
| `Object invoke(Object obj, Object... args)` | 调用指定方法                                    |
| 参数一：`obj`                               | 要调用方法的对象实例（若为静态方法则传 `null`） |
| 参数二：`args`                              | 传递给方法的参数，可为空（无参方法无需传入）    |
| 返回值                                      | 方法的执行结果（无返回值时返回 `null`）         |

## 🔗 相关链接

- [[Java/Javase/动态代理|动态代理]] — 基于反射实现代理模式
- [[Java/Javase/Java加强01|Java加强01]] — 泛型详解
- [[Java/Javaweb/Spring AOP|Spring AOP]] — AOP 底层基于动态代理
